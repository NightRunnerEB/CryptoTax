rabbitmq_publisher:
  host: localhost
  port: 5672
  user: guest
  password: guest
  exchange: cryptotax
  routing_key: import.completed
  reconnect_attempts: 100
  reconnect_timeout_ms: 5000

outbox_worker:
  batch_size: 10
  max_attempts: 10
  poll_interval: 5

[package]
name = "cache"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "1"
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
bb8-redis = { version = "0.23", optional = true }
bb8 = { version = "0.9", optional = true }

[features]
default = ["cache_redis"]
cache_redis = ["dep:bb8", "dep:bb8-redis"]
